/**
 * Native stub — Checkout.js is web-only. Store builds use `startYooKassaPayment` (Gap G).
 */

export function YooKassaCheckoutWidget(_props: {
  transactionId: string
  confirmationToken?: string | null
  onCompleted?: (transactionId: string) => void
  onFailed?: (message: string) => void
}) {
  return null
}
